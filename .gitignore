__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.env
.venv
env/
venv/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Phase-3 Go daemon build output.
netd/bin/

# TLA+ TLC scratch state (per-run state queue + checkpoints)
spec/states/

# Rust workspace build output.
gyza-rs/target/

# Claude Code per-project local state.
.claude/

# Deploy-time secrets (ANTHROPIC_API_KEY etc) sourced by
# scripts/deploy-bootstrap.sh. Never commit this.
scripts/deploy.env

# Editor / OS droppings.
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Internal working docs — kept locally, not published (launch hygiene)
CLAUDE.md
CHANGELOG.md

# Internal engineering docs — kept locally, not published pre-launch
docs/
gyza-rs/MIGRATION.md
spec/
