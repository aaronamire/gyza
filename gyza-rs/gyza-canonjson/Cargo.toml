[package]
name = "gyza-canonjson"
description = "Canonical JSON bytes byte-identical to Python's json.dumps(sort_keys=True, separators=(',',':')). Exists so exactly ONE implementation of the escaping rule can drift."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
